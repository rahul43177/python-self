"""
When working with real-world data in your organization, things will go wrong. A database might be down, a file might be missing, or a dictionary might be missing a key.

Instead of letting the whole program crash, we use try...except blocks to catch the "exception" and handle it gracefully. 🩹

"""